# Find id of Elantech Touchpad
xinput --list
# Find property id n, probably called «Disable While Typing Enabled»:
xinput --list-props <m>
# Deactivate it:
xinput --set-prop <m> <n> 0

# Install non free nvidia 
sudo mhwd -a pci nonfree 0300
# Install free nvidia
sudo mhwd -a pci free 0300

# Check GPUs
inxi -G

# RUN STEAM WITH "prime-run steam"
